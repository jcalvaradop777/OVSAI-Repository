export const metadata = {
  title: "OVSAI",
  description: "SGC",
};

export default function RootLayout({ children }) {
 return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
